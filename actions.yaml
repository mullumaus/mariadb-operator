restart:
  description: Restart mariadb
  
backup:
  description: Perform a database backup

restore:
  description: Perform a database restore
  params:
    path:
      description: |
        The backup file name to restore, e.g.20210521-123532-backup.sql.gz. 
        If not specified, the latest backup will be resotre
      type: string
      default: ""

listbackup:
  description: To list all backup files